# NOTAS:

Aqui los comandos disponibles del programa

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es el numero base de la tabla de multiplicar
                                                            [número] [requerido]
  -l, --listar   Muestra la tabla en copnsola        [booleano] [defecto: false]
  -h, --hasta    Indica el numero al cual se debe calcular el la base
                                              [número] [requerido] [defecto: 10]


```